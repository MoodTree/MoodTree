<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.moodtree.mappers.MemberMapper">

    <!-- 회원가입 -->
    <insert id="insertMember" parameterType="com.moodtree.domain.Member">
        INSERT INTO Member (
        email, name, phone, nickname,
        password, created_at, updated_at)
        VALUES (
        #{email}, #{name}, #{phone}, #{nickname},
        #{password}, NOW(), NOW()
        )
    </insert>




</mapper>